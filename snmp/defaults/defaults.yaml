service: snmpd
servicetrap: snmptrapd
config: /etc/snmp/snmpd.conf
configtrap: /etc/snmp/snmptrapd.conf
configagent: /etc/snmp/snmp.conf
mibsdir: /usr/share/snmp/mibs
source: salt://snmp/files/snmpd.conf
sourcetrap: salt://snmp/files/snmptrapd.conf
sourceagent: salt://snmp/files/snmp.conf
trapdargs: -Lsd -p /var/run/snmptrapd.pid
rootgroup: root
system_location: Somewhere over the Rainbow
system_contact: admins@snmp.formulas.salt
v3users:
  testuser1:
    authpass: ASDdfg789
    privpass: ASDdfg789
  testlegacyadmin:
    authpass: ASDdfg789
    privpass: ASDdfg789
    read_only: False
    auth_hash_sha: False
    encryption_aes: False
    